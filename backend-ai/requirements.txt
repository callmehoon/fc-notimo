# 웹 프레임워크 및 서버
# =================================
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.4                  # 데이터 유효성 검사 및 스키마 정의
websockets==12.0                 # 실시간 챗봇 통신용
python-multipart==0.0.9          # HTML Form을 통한 파일 업로드용

# AI / LLM 핵심 라이브러리
# =================================
openai==1.30.1                   # OpenAI API 사용
transformers==4.41.2             # Hugging Face의 사전 학습된 모델(LLM) 사용
torch==2.3.1                     # Transformers의 백엔드 딥러닝 프레임워크 (PyTorch)
accelerate==0.30.1               # 모델 로딩 및 추론 가속화
sentence-transformers==3.0.1     # 문장 임베딩 및 의미 유사도 계산

# LangChain 프레임워크
# =================================
langchain==0.2.1                 # LLM 기반 애플리케이션 개발 프레임워크
langchain-openai==0.1.7          # LangChain과 OpenAI 모델 연동
langchain-community==0.2.1       # LangChain의 커뮤니티 기반 연동 기능

# 벡터 데이터베이스
# =================================
chromadb==0.5.0                  # 임베딩 저장을 위한 벡터 DB 클라이언트

# 데이터베이스 드라이버
# =================================
mysql-connector-python==8.4.0    # MySQL 연결용 공식 드라이버

# 유틸리티 및 데이터 처리
# =================================
numpy==1.26.4                    # 다차원 배열 및 수학 연산
python-dotenv==1.0.1             # .env 파일 로드용

# 테스트
# =================================
# pytest는 실제 서비스 실행에는 필요 없는 개발용 라이브러리입니다.
pytest==8.2.1
